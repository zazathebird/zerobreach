# _deferred — not for this session

These briefs belong to a different work package: the detection modules for phases 146–159, owned
by the main session. They are parked here only so the two packages stay in one folder.

**Do not open these files in a session working out of `fable-work/`.** They describe engine
internals that nothing in `tasks/` touches, they will pull a large amount of unrelated material
into context, and any work done from them collides directly with what the main session is
editing.

If you were sent here by a task in `tasks/`, that is a mistake in the task — say so.
